# Expo EAS Build APK 构建指南

## 概述

Expo EAS Build 是 Expo 提供的云端构建服务，可以自动构建 Android APK 和 iOS IPA 文件，无需本地配置复杂的开发环境。

## 前置条件

- [x] 已安装 Node.js (v18+)
- [x] 已安装 Expo CLI
- [x] 已登录 Expo 账户
- [x] 项目已配置 EAS

## 步骤 1: 确认登录状态

在终端中执行：

```bash
eas whoami
```

如果显示你的用户名，说明已登录。
如果显示 "Not logged in"，执行：

```bash
eas login
```

## 步骤 2: 构建 APK

### 方式 A: 使用 PowerShell 脚本（推荐）

1. 关闭当前 PowerShell 窗口
2. 打开一个新的 PowerShell 窗口（**重要！**）
3. 执行：

```powershell
cd E:\WarehouseScanner
```

4. 然后执行构建命令：

```powershell
eas build --platform android --profile production
```

### 方式 B: 直接执行命令

在 PowerShell 中执行：

```powershell
cd E:\WarehouseScanner
eas build --platform android --profile production
```

### 方式 C: 使用我创建的脚本

双击运行：

```
E:\WarehouseScanner\eas_build_apk.bat
```

## 步骤 3: 等待构建完成

构建过程通常需要 **10-30 分钟**，取决于项目大小和服务器负载。

构建过程中会显示：
- ✅ 安装依赖
- ✅ 准备构建
- ✅ 构建中
- ⏳ 等待完成

## 步骤 4: 下载 APK

构建成功后，你有两个选择：

### 选择 1: 通过命令行下载

构建完成后，终端会显示下载链接。

### 选择 2: 通过 Expo 官网下载

1. 访问 https://expo.dev/builds
2. 找到你的构建任务
3. 点击 "Download" 下载 APK

### 选择 3: 通过邮件下载

构建完成时，Expo 会发送下载链接到你的邮箱。

## 配置文件说明

你的项目已配置三个构建配置：

| 配置 | 命令 | 用途 |
|------|------|------|
| development | `eas build --profile development` | 开发版本，包含开发客户端 |
| preview | `eas build --profile preview` | 预览版本 |
| **production** | `eas build --profile production` | **生产版本，推荐使用** |

生产版本 (production) 生成的是正式可发布的 APK 文件。

## 安装 APK 到手机

1. 将下载的 APK 文件传输到手机
2. 在手机上打开文件管理器
3. 点击 APK 文件进行安装
4. 如果系统提示"未知来源"，前往设置 > 安全 > 允许未知来源

## 常见问题

### Q: 构建失败怎么办？

1. 查看错误日志：
   ```bash
   eas build:inspect
   ```

2. 常见问题：
   - 依赖版本冲突
   - 内存不足
   - 网络超时

### Q: 如何查看构建进度？

访问：https://expo.dev/builds

### Q: 可以构建 iOS 版本吗？

可以，执行：
```bash
eas build --platform ios --profile production
```

注意：iOS 构建需要 Apple 开发者账户。

### Q: 之前的 GitHub Actions 构建会冲突吗？

不会。GitHub Actions 和 EAS Build 是两个独立的构建系统，可以同时存在。

### Q: 需要推送代码到 GitHub 吗？

**不需要！** EAS Build 直接从你的本地代码构建，无需推送到 GitHub。

## 下一步操作

1. **打开 PowerShell**
2. **执行构建命令**：
   ```powershell
   cd E:\WarehouseScanner
   eas build --platform android --profile production
   ```
3. **等待 10-30 分钟**
4. **下载 APK**

## 快速参考

```bash
# 1. 进入项目目录
cd E:\WarehouseScanner

# 2. 确认登录
eas whoami

# 3. 构建 Android APK（生产版本）
eas build --platform android --profile production

# 4. 或者构建预览版本
eas build --platform android --profile preview

# 5. 查看构建状态
eas build:list
```

## 预期结果

- **构建状态**: Success (绿色)
- **构建产物**: app-production.apk 或 app-release.apk
- **文件大小**: 约 30-50MB

---

**祝你构建成功！** 🎉

如果遇到问题，请告诉我错误信息，我会帮你解决。
